apiVersion: apps/v1
kind: Deployment
metadata:
  name: expedition-service
  namespace: redbus
spec:
  replicas: 2
  selector:
    matchLabels:
      app: expedition-service
  template:
    metadata:
      labels:
        app: expedition-service
    spec:
      containers:
      - name: expedition-service
        image: rahulraman/redbus-expedition-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_APPLICATION_NAME
          value: expedition-service
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/expeditionDB
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            configMapKeyRef:
              name: redbus-config
              key: POSTGRES_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: redbus-secrets
              key: POSTGRES_PASSWORD
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: redbus-config
              key: EUREKA_SERVER
        - name: LOGGING_LEVEL_ROOT
          value: "INFO"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 40
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 5
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 600m
            memory: 1Gi
      imagePullSecrets:
      - name: docker-credentials
---
apiVersion: v1
kind: Service
metadata:
  name: expedition-service
  namespace: redbus
spec:
  selector:
    app: expedition-service
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP
